from django.conf.urls import url, include
import views

urlpatterns = [
    
    url(r'^all/$', views.tests),
    url(r'^(?P<test_id>\d+)/$', views.quetions),
    url(r'^testing/(?P<test_id>\d+)/(?P<quetion_id>\d+)/$', views.testing),
    url(r'^result/$', views.result),
    
] 
